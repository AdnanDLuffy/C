#include <stdio.h>

int main() {
    char a = 'a';
    char multiple[] = "Adnan is a good boy";

    // [] this let's store a whole line
    // this %s is a format specifier
    printf("%s",multiple);

    return 0;
}